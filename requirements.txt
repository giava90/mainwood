# Runtime dependencies of the ForClim -> SorSim pipeline.
#
# On Euler these come from the module stack, not from pip:
#   module load stack/2024-06 python/3.12.8
#   module load stack/2024-06 openjdk/21.0.3_9      # SorSim is a Java jar
#
# Locally:  conda create -n mainwood python=3.12 --file requirements.txt
#           (or: pip install -r requirements.txt)

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1        # reading data/fraction_quality.xlsx
pyarrow>=14.0        # Parquet summaries (code/summary_io.py, summary_to_csv.py)
py4j>=0.10.9         # bridge to minimal/sorsim/SorSim4Python.jar

# Tests only
pytest>=7.0
